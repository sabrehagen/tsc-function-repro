{
  "name": "tsc-function-name",
  "version": "1.0.0",
  "description": "Repro for function names being stripped by typescript compiler",
  "license": "MIT",
  "scripts": {
    "test": "yarn test:native; yarn test:compiled",
    "test:compiled": "yarn; yarn tsc --project tsconfig.json index.ts 2>&1 >/dev/null; node index.js",
    "test:native": "yarn; node --experimental-modules index.mjs 2>/dev/null"
  },
  "devDependencies": {
    "typescript": "^3.6.3"
  }
}
